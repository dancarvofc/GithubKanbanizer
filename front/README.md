# Frontend (React) - GitHub Kanbanizer

Rápido scaffold Vite + React + TypeScript + Tailwind com React Query e Zustand.

Para rodar:

```bash
cd front
pnpm install # or npm install
pnpm dev
```

Variáveis (opcional):
- `VITE_API_URL` — URL do backend (padrão `http://localhost:3001`)
