📑 Documentação de Arquitetura: GitHub Kanbanizer
1. Stack Tecnológica Final

Frontend (A Camada Visual)

Vite + React: Build ultra-rápido e ambiente moderno.
Tailwind CSS + Shadcn/UI: Componentização visual profissional e rápida.
Framer Motion: Responsável pela fluidez. Entrada dos cards, troca de colunas e feedbacks visuais.
TanStack Query: Gerenciamento de estado assíncrono e cache das respostas da API.
Lucide React: Ícones minimalistas.
Date-fns: Tratamento de datas para o cálculo de maturidade ($\Delta t$).
Zustand: Guardando infos Globais

Backend (A Camada de Inteligência)

Fastify: Servidor de alta performance e baixo overhead.
Octokit: SDK oficial do GitHub para integração estável.
Node-cache: Estratégia de cache em memória (Server-side) para evitar Rate Limit.
Swagger (fastify-swagger): Documentação automática da sua API para testes rápidos.


2. Padrões de Projeto e Camadas
Utilizaremos uma arquitetura separada por Responsabilidades Únicas, facilitando a manutenção.


backend/
├── src/
│   ├── routes/          # Definição dos endpoints (Swagger aqui)
│   ├── controllers/     # Recebe a requisição e valida com Zod
│   ├── services/        # Regra de negócio (Cálculo de Maturidade)
│   ├── providers/       # Integração com Octokit (GitHub)
│   └── lib/             # Configurações globais (Cache, Fastify inst.)


frontend/
├── src/
│   ├── components/      # UI (Shadcn) e Business Components (Card, Column)
│   ├── hooks/           # useGitHubData (TanStack Query encapsulado)
│   ├── store/           # Zustand (Estado do usuário buscado)
│   ├── utils/           # Lógica de formatação de datas
│   └── styles/          # Configuração Tailwind


Essa escolha de stack está muito madura. Você está focando em performance e DX (Developer Experience).

Sobre suas dúvidas:

Zustand: Se o app for só uma página, o useState resolve. Mas o Zustand é bom para guardar o "Username" globalmente sem fazer prop drilling.

React Cache: O React não tem um cache de rede nativo robusto. O TanStack Query é o "cérebro" que impede que seu app dê F5 e gaste outra requisição à toa.

Zod: Mesmo sem formulários, ele é ótimo no Back para validar o que o GitHub te envia e garantir que seu código não quebre se um campo vier null.

Octokit: Facilita muito. Ele já sabe lidar com os cabeçalhos de autenticação e erros específicos do GitHub sem você configurar nada.

Aqui está o Documento de Arquitetura do GitHub Kanbanizer:

📑 Documentação de Arquitetura: GitHub Kanbanizer
1. Stack Tecnológica Final
Frontend (A Camada Visual)
Vite + React: Build ultra-rápido e ambiente moderno.

Tailwind CSS + Shadcn/UI: Componentização visual profissional e rápida.

Framer Motion: Responsável pela fluidez. Entrada dos cards, troca de colunas e feedbacks visuais.

TanStack Query: Gerenciamento de estado assíncrono e cache das respostas da API.

Lucide React: Ícones minimalistas.

Date-fns: Tratamento de datas para o cálculo de maturidade (Δt).

Backend (A Camada de Inteligência)
Fastify: Servidor de alta performance e baixo overhead.

Octokit: SDK oficial do GitHub para integração estável.

Node-cache: Estratégia de cache em memória (Server-side) para evitar Rate Limit.

Zod: Validação de schemas e tipagem estática.

Swagger (fastify-swagger): Documentação automática da sua API para testes rápidos.

2. Padrões de Projeto e Camadas
Utilizaremos uma arquitetura separada por Responsabilidades Únicas, facilitando a manutenção.

Backend (Estrutura de Pastas)
Plaintext
backend/
├── src/
│   ├── routes/          # Definição dos endpoints (Swagger aqui)
│   ├── controllers/     # Recebe a requisição e valida com Zod
│   ├── services/        # Regra de negócio (Cálculo de Maturidade)
│   ├── providers/       # Integração com Octokit (GitHub)
│   └── lib/             # Configurações globais (Cache, Fastify inst.)
Frontend (Estrutura de Pastas)
Plaintext
frontend/
├── src/
│   ├── components/      # UI (Shadcn) e Business Components (Card, Column)
│   ├── hooks/           # useGitHubData (TanStack Query encapsulado)
│   ├── store/           # Zustand (Estado do usuário buscado)
│   ├── utils/           # Lógica de formatação de datas
│   └── styles/          # Configuração Tailwind


3. Fluxo de Dados (Diadático)

1- Entrada: O usuário digita o username no Frontend.

2- Request: O React chama seu Backend Fastify: GET /api/kanban/:username.

3- Cache Check: O Backend verifica o node-cache.

    3a -Tem no cache? Retorna imediatamente.

    3b -Não tem? Segue para o próximo passo.

4- Integration: O Octokit busca os dados do Usuário (Profile) e dos Repos em paralelo.

5- Data Processing (A Mágica):

5a- O Service filtra os repos (remove forks, se desejar).

    5b- Calcula o status (Doing, To Do, Done) usando date-fns.

    5c- Monta o objeto final limpo.

6- Response: Retorna o JSON estruturado para o Frontend.

7- Render: O TanStack Query armazena o resultado e o Framer Motion anima a entrada dos cards na tela.


4. Por que essa arquitetura é "Comercial"?
->  Escalabilidade: O uso do Fastify e Node-cache permite que o app suporte muitos acessos sem travar por limites do GitHub.

->  Resiliência: Com Zod e TanStack Query, o app lida graciosamente com erros (ex: usuário não encontrado) sem "quebrar" a tela branca.

->  Apresentação: O Shadcn + Framer Motion entrega um visual de produto premium, essencial para um portfólio de alto nível.